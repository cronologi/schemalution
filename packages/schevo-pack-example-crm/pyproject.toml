[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "schevo-pack-example-crm"
version = "0.0.1"
description = "Example CRM schema pack for Schevo."
requires-python = ">=3.10"
dependencies = ["schevo-core", "schevo-pack"]

[tool.hatch.build.targets.wheel]
packages = ["schevo_pack_example_crm"]
