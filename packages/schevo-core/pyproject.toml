[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "schevo-core"
version = "0.0.1"
description = "Core interfaces for Schevo."
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["schevo_core"]
