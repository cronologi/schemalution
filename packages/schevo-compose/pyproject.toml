[build-system]
requires = ["hatchling>=1.21"]
build-backend = "hatchling.build"

[project]
name = "schevo-compose"
version = "0.0.1"
description = "Composition helpers placeholder for Schevo."
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["schevo_compose"]
